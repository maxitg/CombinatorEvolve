@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

get_filename_component(COMBINATOR_EVOLVE_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
if(NOT TARGET CombinatorEvolve)
  include ("${COMBINATOR_EVOLVE_CMAKE_DIR}/CombinatorEvolveTargets.cmake")
endif()
